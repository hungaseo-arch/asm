# 보고 — Phase 1 (Neon 스키마 · RLS · 트리거)

> 작성일 2026-09-10 · 작업지시서 §7 · 저장소 `hungaseo-arch/asm` (`D:\asm-web-vue`)

---

## 1. §0 확인 결과

| # | 항목 | 결과 | 근거 |
|---|---|---|---|
| C-1 | 저장소 로컬 경로 | ✅ `D:\asm-web-vue` | `git remote -v` → `https://github.com/hungaseo-arch/asm.git` |
| C-2 | 라우트 명칭 | ✅ **`/csr`** 확정 | 소스너 확정 2026-09-10. Notion DB 명칭·이슈번호 체계와 통일 |
| C-3 | Neon 프로젝트 | ✅ 생성·설정 완료 | `asm-csm` · org `org-lucky-forest-86169045` · Singapore. Neon Auth + Data API 활성 확인 (2026-09-10) |
| C-4 | Drive 캡쳐 폴더 | ✅ **확정** | `내 드라이브/CSR_캡쳐` = id `1iEalwwo8BOP4HQnO6HQ8H_XYlhnwnGdV` (2026-09-10 소스너 생성). 공유 범위 설정만 남음 |
| C-5 | Notion 원본 | ✅ 실재·스키마 일치 | 데이터소스 `5e2c4ff4-…001145`, 속성명이 §4-3 매핑표와 일치 |
| C-6 | 계정 3종 이메일 | ⏸ **보류** | 계정을 나중에 생성하기로 확정(2026-09-10). `db/004_seed_roles.sql` 상단 TODO 3줄 유지 |

### §4-4 검증 기준 사전 대조 (Phase 2 종료 조건)

지시서 수치와 **정확히 일치**함을 원본에서 확인했습니다.

| 상태 | 지시서 | 실측 |
|---|---|---|
| Open | 35 | **35** |
| Ongoing | 14 | **14** |
| Completed | 5 | **5** |
| Verified | 6 | **6** |
| N/A | 3 | **3** |
| 합계 | 63 | **63** |

`(삭제)` 표기 레코드는 3건이며 전부 N/A 상태입니다 → `is_archived = true` 3건, 화면 표시 대상 60건.

### 추가로 확인된 사항

- **작업지시서 §6 의 「회원가입 비활성」은 현재 불가능합니다.** Neon Auth 화면이
  「Anyone on the web can sign up … restricted signups is coming soon」 이라고 명시합니다.
  다만 설계상 방어가 이미 있습니다 — 모든 SELECT 정책이 `csr_role() IS NOT NULL` 이라
  `csr_user_roles` 에 없는 사용자는 **가입해도 이슈를 한 건도 볼 수 없습니다.**
  남는 위험은 데이터 노출이 아니라 계정 증가이며, Neon 이 기능을 내놓으면 켭니다.
- **Neon Auth 는 Better Auth 기반**입니다. 이 저장소에 이미 `better-auth` 의존성이
  있어(package.json) Phase 3 에서 재사용 여지가 있습니다.
- **`auth.user_id()` 함수명은 아직 실물로 확인하지 못했습니다.** `002` 의 `csr_role()` 은
  생성 시점에 파싱되므로 이름이 다르면 즉시 실패합니다 — `db/000_preflight.sql` 로
  먼저 확인하도록 절차를 추가했습니다.

- **§5-1 의 GitHub Pages 404 문제는 이미 해결되어 있습니다.** 라우터는
  `createWebHistory(import.meta.env.BASE_URL)`(base `/asm/`)이고,
  [deploy.yml](../../.github/workflows/deploy.yml) 이 `cp dist/index.html dist/404.html` 을 수행합니다.
- **`asm-common.css` 는 이름만 다르게 존재합니다** — [asm-theme.css](../../src/assets/asm-theme.css).
  Bootstrap 5.3 도 있으므로 §7 의 「목적 저장소가 Bootstrap 5 가 아니거나 asm-common.css 부재」
  중단 조건에는 해당하지 않습니다.

---

## 2. 확인 중 드러난 지시서-저장소 충돌과 처리

| 충돌 | 내용 | 처리 (소스너 확정) |
|---|---|---|
| 언어 | 지시서 §5-1 은 `.ts`, workOrder.md §2 는 **TypeScript 도입 금지** (커밋 `e0c8461` 에서 JS 전환 완료) | **JavaScript 로 작성.** §6 의 `vue-tsc --noEmit` 은 실행 불가라 `vite build` + `prettier --check` 로 대체 |
| 식별자 | 라우트가 `/csr` 인데 §3 SQL 은 `csm_` 접두사 | **`csr_` 로 통일** — 테이블·함수·정책·정책상수(`CSR_POLICY`) 전부 |
| 의존성 | `@neondatabase/neon-js` 는 신규 의존성 (workOrder.md §9 승인 대상) | **승인 완료.** 실제 설치는 Phase 3 |
| 참고 코드 | mobilPress 가 이 PC 에 없음 (§7 즉시 중단 조건) | **Phase 1 은 참고 코드가 필요 없어 진행.** 코드 이식이 실제로 필요한 Phase 3 착수 전에 다시 요청 |

---

## 3. 완료 목록

### 작성한 SQL (적용은 소스너)

| 파일 | 내용 |
|---|---|
| `db/001_schema.sql` | 테이블 6종(`csr_user_roles` `csr_issues` `csr_it_replies` `csr_verifications` `csr_attachments` `csr_status_log`) · 인덱스 6종 · 채번 함수 `csr_next_issue_no()` |
| `db/002_rls_policies.sql` | 역할 헬퍼 `csr_role()` · RLS 활성 6종 · 정책 16종 · Data API `authenticated` GRANT |
| `db/003_triggers.sql` | 컬럼 가드 `csr_issues_column_guard` (BEFORE UPDATE) · 상태 로그 `csr_issues_status_log` (AFTER UPDATE) |
| `db/004_seed_roles.sql` | 역할 3종 등록 — **C-6 대기 중(TODO 3줄)** |

### 문서

| 파일 | 내용 |
|---|---|
| `docs/csr/이관절차_콘솔설정.md` | Neon 프로젝트 생성 · SQL 적용 순서 · 계정 3종 · 적용 후 점검 4종 · Drive 폴더 |
| `docs/csr/보고_Phase1_20260910.md` | 이 문서 |

### 설계상 짚어 둔 점

- **`csr_role()` 은 `SECURITY DEFINER`** 입니다. `csr_user_roles` 에도 RLS 가 걸려 있고 그
  정책이 다시 `csr_role()` 을 부르므로, 호출자 권한으로 두면 무한 재귀가 됩니다.
- **컬럼 가드는 '실제로 값이 달라진 컬럼'만 셉니다.** 화면이 폼 전체를 되보내도 안 바뀐
  값은 위반이 아닙니다 — 그러지 않으면 it_dept 가 회신만 고쳐도 거부됩니다.
- **상태 로그 트리거도 `SECURITY DEFINER`** 입니다. `csr_status_log` 에는 INSERT 정책을
  두지 않아 Data API 로는 아무도 로그를 쓰거나 지울 수 없고, 적재는 트리거만 합니다.
- **RLS 정책과 GRANT 는 둘 다 필요합니다.** 정책만 넣고 GRANT 를 빠뜨리면 조회가 빈 결과나
  401/403 으로 나옵니다(§7 즉시 중단 조건). 002 가 `authenticated` 역할이 없으면
  `NOTICE` 로 알려 주고 넘어가므로, **Data API 를 켠 뒤 002 를 다시 실행**하면 됩니다.
- 모든 파일은 **여러 번 실행해도 안전**합니다(`IF NOT EXISTS` · `DROP POLICY IF EXISTS`).

---

## 4. 미완 · 보류 항목과 사유

| 항목 | 사유 | 해소 시점 |
|---|---|---|
| C-6 계정 3종 이메일 | 소스너가 나중에 생성하기로 함 | 계정 생성 시 `004_seed_roles.sql` 3줄만 교체 |
| Drive 폴더 **공유 범위** | 폴더는 생성 완료(id `1iEalwwo8BOP4HQnO6HQ8H_XYlhnwnGdV`). 공유 대상 계정이 아직 없음 | C-6 계정 생성 직후 |
| **SQL 실행 검증** | 이 PC 에 Postgres·Docker 가 없어 **문법을 실행으로 확인하지 못했습니다** | Neon 콘솔 첫 적용 시 확인. 원하시면 임시 인스턴스로 사전 검증 가능 |
| mobilPress 대조 (§2 표) | 저장소 부재 | Phase 3 착수 전 |
| `@neondatabase/neon-js` 설치 | Phase 3 범위 | Phase 3 |

> **가장 중요한 잔여 리스크는 SQL 미실행입니다.** 파일은 문법을 주의해 작성했지만
> 실제 Postgres 에서 돌려 보지 못했으므로, §2 절차대로 **한 파일씩** 결과를 보며
> 적용하시기 바랍니다. 각 파일이 트랜잭션으로 묶여 있어 실패해도 되돌아갑니다.

---

## 5. 변경한 파일 목록

| 파일 | 구분 |
|---|---|
| `db/000_preflight.sql` | 신규 — 읽기 전용 선행 점검 |
| `db/001_schema.sql` | 신규 |
| `db/002_rls_policies.sql` | 신규 |
| `db/003_triggers.sql` | 신규 |
| `db/004_seed_roles.sql` | 신규 |
| `docs/csr/이관절차_콘솔설정.md` | 신규 |
| `docs/csr/보고_Phase1_20260910.md` | 신규 |
| `.gitignore` | 수정 — `import/` 추가 (§4-1, Notion 원본 커밋 금지) |

애플리케이션 코드(`src/`)는 **한 줄도 건드리지 않았습니다.** Phase 3 범위입니다.

---

## 6. 소스너 실행 필요 사항

1. ~~Neon 프로젝트 생성 → Neon Auth → Data API 활성화~~ ✅ 2026-09-10 완료
2. **`db/000_preflight.sql` 실행** — `auth.user_id()` 함수명 확인 (읽기 전용)
3. `db/001` → `002` → `003` 순서로 적용, 각 파일 후 §4 점검 실행
4. Data API 화면에서 **Refresh schema cache** 클릭
5. Auth URL 기록 (Phase 3 `.env` 용 — Data API URL 은 확보 완료)

계정이 준비된 뒤에 (보류):

6. C-6 이메일 3개 확정 → Neon Auth 계정 생성 → `004` 적용
7. Drive `CSR_캡쳐` 폴더를 계정 3종에만 공유

절차 전문: [이관절차_콘솔설정.md](이관절차_콘솔설정.md)

---

## 7. 다음 Phase 착수 조건

**Phase 2 (Notion → SQL 이관)** 를 시작하려면:

- [ ] `db/001`~`003` 적용 완료 (`004` 는 계정 생성 후로 보류 — Phase 2 를 막지 않습니다)
- [ ] Notion 내보내기 zip 을 `import/notion_export/` 에 배치 (§4-1)
      — CSV 1개 + 페이지 md 63개 + 이미지 폴더
- [x] Drive `CSR_캡쳐` 폴더 생성 — id `1iEalwwo8BOP4HQnO6HQ8H_XYlhnwnGdV` (2026-09-10 완료)

> 참고: Notion 데이터소스에 직접 접근할 수 있으므로, 수동 내보내기 대신 **원본을 직접 읽어
> INSERT SQL 을 생성하는 방식**도 가능합니다. 이미지(§1 캡쳐)는 내보내기 파일이 필요하지만
> 속성·본문은 직접 조회로 처리할 수 있어 63건 대조가 더 정확해집니다.
> 어느 쪽으로 할지는 Phase 2 착수 시 확정하겠습니다.

**Phase 1 종료 — 중단하고 대기합니다.**
