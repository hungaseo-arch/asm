# CSR 상세 70건 한·인니 전환 완성 — 작업지시서 (2026-09-19)

> 대상: Claude Code (`D:\asm-web-vue`) · 적용자: 서종환
> 원칙: 저장값 정규화 금지(§8) · Notion 속성명·IT 코드 변경 금지 · 컬럼 추가 없음(010·014 쌍 재사용)

## 1. 현황 (2026-09-19 저장소 `eb198ff` 기준)
- 전환 기능은 이미 있음 — `CsrLangToggle` + `i18n.js`(라벨·용어 사전) + `pairOf/bodyOf`(토글 언어 → 반대쪽 → 원문).
- 데이터 쌍: 이슈 본문 `findings_md/recommendation_md/business_answer_md` `_ko/_id`(010, 01~60) · 61~68(024) · 69·70(019·020) → **전건 보유**.
  검증 이력 `note_ko/_id`(014, id 38~255 · 146행) · IT 회신 4항목 쌍(014, 42행).
- 누락: **사이트 폼으로 입력된 행** — 「+ 검증 추가」 「+ 회신 추가」 폼이 단일 칸이라 `note` 만 저장되어 토글해도 같은 글이 보임.
  2026-09-10 이후 입력분(SEO 09-18 19행 · Lestari/Lia/Merry 09-15~18 약 6행).

## 2. 코드 — `src/modules/csr/views/CsrDetailPage.vue` (patch `034_csr_detail_bilingual_forms.patch`)
1. §7 검증 추가 폼: 「내용」 단일 칸 → **KO / ID 두 칸**(`note_ko` · `note_id`), 원문 `note` = KO 우선(없으면 ID). 본문 편집(`saveMd`)과 같은 규칙.
2. §5 IT 회신 폼: `fix_plan · note · needs_decision · pic_and_target` 4항목 → 각 **KO / ID 두 칸**, `<col>_ko/_id` + 원문 `<col>`.
3. 공통 헬퍼 `pairFields(draft, keys)` 추가. API(`api/issues.js`)는 필드를 그대로 insert 하므로 변경 없음. RLS·컬럼 가드 영향 없음(002 GRANT INSERT · 015b 는 csr_issues 전용).
4. 확인: `npm run build` 통과(2026-09-19 컨테이너 빌드 확인) · prettier 통과.

적용: `git apply 034_csr_detail_bilingual_forms.patch` (또는 db/032·033·docs 포함 전체) → `git push origin main` → Pages 자동 배포.

## 3. 데이터 — Neon 콘솔 SQL Editor(asm-csm · production)
1. `db/033_verif_bilingual_fill_20260919.sql` 실행 — 2026-09-18 SEO 입력 19행(02·07·09·10·11·13·18·40·44·52·53·59·63~69)의 `note_ko/_id` 채움. 재실행 안전.
2. `db/032_bilingual_gap_report.sql` 실행(읽기 전용) — 4개 결과 표를 그대로 복사해 Claude 에 전달 → 잔여분(Lestari·Lia·Merry 입력 행 등) 번역 SQL `035_…` 작성.
3. 032 결과가 0행이면 70건 상세 전환 완료.

## 4. 경과 (2026-09-19 실행 기록)
- 코드 `e1406e3` 적용·배포. `033` 실행 — 19행 모두 `UPDATE 1`.
- `032` 1차 결과: 이슈 본문 4행 · 검증 이력 12행 · IT 회신 3행.
  - **진짜 누락은 검증 이력 10행뿐**이었습니다(09-10~11 b969bcec 9행 + 09-18 Lestari 02번 1행). `note_ko`·`note_id` 가 **양쪽 다 비어** 있고 인니어 원문 `note` 만 있던 행 → `035` 로 원문을 `note_id` 에, 번역을 `note_ko` 에 넣습니다. 키는 `verif_id`.
  - 나머지 9행은 오탐이었습니다. 두 가지 원인이라 `032` 의 판정 규칙을 고쳤습니다:
    · 빈칸 표기(`- —` · `N/A`) 제외 규칙이 §3 에만 있었음 → §1·§2·§4 에도 적용. 단, 원문이 있는데 쌍만 빈 행은 계속 잡히도록 원문 컬럼(`src`)을 기준으로 판단합니다.
    · `id ~ '[가-힣]'` 가 인니어 본문 속 한국어 UI 문구 인용(`'연도-월-일'`)까지 잡았음 → 한글이 전체 길이의 10%를 넘을 때만 `WRONG_LANG`. 양쪽이 같은 언어인 진짜 사고는 100%에 가까워 그대로 잡힙니다.
  - 고친 `032` 는 로컬 Postgres 픽스처로 확인했습니다 — 오탐 5종은 빠지고 진짜 누락 4종(원문만 있는 본문·검증·회신, 양쪽 한국어인 요약)은 그대로 잡힙니다.
- `035` 실행 후 고친 `032` 재실행 — **4개 표 모두 0행. 상세 70건 한·인니 전환 완료(2026-09-19).**
- 02번 `verif_id` 284(Lestari, 09-18, 공장 발송 PO 출력물 통화 기호)는 **PENDING 으로 확정**(2026-09-19 서종환). 별도 CSR 로 빼지 않고 02 안에 두며, 필요해지면 그때 수동으로 고칩니다. `035` 는 번역만 채우고 `result` 는 건드리지 않았습니다.
- 화면 확인(2026-09-19): `/csr/13` · `/csr/40` 양쪽 토글 정상. 특히 `/csr/13` 2026-09-10 행이 035 이전에는 한국어 토글에서도 인니어로 보이던 행입니다.

## 5. 덧붙임 — 검증 이력 「IT 상태」 컬럼 (2026-09-19)
- §7 검증 이력 표에 **검증 시점의 IT 상태** 열을 넣었습니다(그 검증이 어느 단계에서 이뤄졌는지가 표에서 보이도록).
- `db/036_verif_it_status_at.sql` — `csr_verifications.it_status_at` 추가 + `csr_status_log` 로 역산 백필. 「+ 검증 추가」 는 이슈의 현재 `it_status` 를 같이 저장합니다(입력칸 없음).
- 실행 결과(2026-09-19): `036` 역산은 **4행**만 채웠습니다 — `csr_status_log` 는 015c 트리거가 UPDATE 때만 적재하므로, 이관 후 `it_status` 가 한 번도 안 바뀐 이슈는 로그 행이 0건입니다.
- `db/037_verif_it_status_at_fallback.sql` — 그런 이슈(로그 0건 = 바뀐 적 없음)는 현재 `it_status` 가 곧 당시 값이라 채웠습니다. **185행**. 추정이 아니라 도출입니다.
- 새 컬럼도 **PostgREST 스키마 캐시**를 타야 화면에 옵니다 — Neon 콘솔 Data API 의 **「Refresh schema cache」** 버튼(030 갱신). 이걸 안 하면 DB 에는 값이 있는데 화면만 빈칸입니다.
- 화면 확인: `/csr/67` 두 행 모두 `OPEN` 배지 표시(2026-09-19).

## 6. 검수 포인트
- `/csr/40` · `/csr/13` 국기 토글 시 검증 이력 마지막 행이 언어별로 바뀌는지.
- 「+ 검증 추가」 에 KO 만 입력 → ID 토글에서도 KO 글이 보이는지(되돌아가기) · 둘 다 입력 → 각각 보이는지.
- 대시보드·내보내기(원문 컬럼 사용)에 빈 값이 생기지 않는지.
