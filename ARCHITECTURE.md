# ARCHITECTURE — ASM Web (Vue 3)

이 문서는 **왜 이렇게 만들었는지**를 적습니다. 무엇이 어디 있는지는 [README.md](README.md),
어떻게 작업하는지는 [CONTRIBUTING.md](CONTRIBUTING.md) 를 보십시오.

기준 문서는 [workOrder.md](workOrder.md) 입니다. 아래 설계는 전부 그 지시서에서 나왔거나,
지시서와 현실이 어긋났을 때 내린 판단입니다.

---

## 1. 한 장 요약

```
                    ┌──────────────────────────────────────────┐
  브라우저          │  Vue 3 SPA (GitHub Pages · base /asm/)   │
                    │  ┌────────────┬─────────────────────┐    │
                    │  │ 운영 화면   │  CSR 모듈 (/csr)     │    │
                    │  │ 25종 + 시안 │  독립 · 공유 없음     │    │
                    │  └─────┬──────┴──────────┬──────────┘    │
                    └────────┼─────────────────┼───────────────┘
                             │                 │
                   axios     │                 │  neon-js (Data API)
                   Bearer    │                 │  + Neon Auth
                             ▼                 ▼
                    ┌────────────────┐  ┌──────────────────────┐
                    │ ASM 실서버      │  │ Neon Postgres        │
                    │ Django REST    │  │ RLS + 컬럼 가드 트리거 │
                    │ (동일 오리진)   │  │ (asm-csm)            │
                    └────────────────┘  └──────────────────────┘
                                              │
                                              ▼  Apps Script 프록시
                                        ┌──────────────┐
                                        │ Google Drive │  캡쳐 이미지
                                        └──────────────┘
```

두 개의 백엔드가 한 SPA 안에 공존합니다. 이유는 §5 에 있습니다.

---

## 2. 스택이 이 조합인 이유

로컬에서 고른 스택이 아니라 **운영 서버(`asm.ascendotyre.com`)를 실측해 맞춘 것**입니다
(workOrder.md §1 — 번들·DOM·응답 헤더 분석).

| 선택 | 근거 |
|---|---|
| Vue 3 + `<script setup>` | 실서버 `#app.__vue_app__.version` = 3.5.41 |
| Vite | 자산 해시 패턴 `assets/index-*.js` |
| Bootstrap 5.3 | `--bs-*` 변수 전량 + 유틸리티 클래스 |
| axios | 번들 문자열 `axios` · `Bearer` · `/api/refresh/` |
| Pinia · Vue Router | `$pinia.state` · `$router.getRoutes()` (83개) |
| Font Awesome · yup · SheetJS | 번들 시그니처 |
| **JavaScript (TS 금지)** | 실서버 번들에 TypeScript 흔적 없음 |

**Tailwind·Element Plus·Vuetify·jQuery 는 금지**입니다. 이관 대상이 아니라 *정합* 대상이기
때문입니다 — 로컬에서 만든 화면이 언젠가 실서버 코드베이스로 들어가야 하므로, 실서버에
없는 의존성을 들이면 그때 전부 걷어내야 합니다.

> TypeScript 는 한 번 도입했다가 커밋 `e0c8461` 에서 전량 JavaScript 로 되돌렸습니다.
> CSR 모듈 지시서가 `.ts` 파일명을 지정했지만 같은 이유로 JS 로 작성했습니다(2026-09-10 확정).

---

## 3. 목록 화면 — 정의를 데이터로 뺀 이유

운영 메뉴는 25종이고 대부분이 "검색 + 기간필터 + 정렬 + 페이징 + 합계 + 엑셀" 이라는
같은 뼈대를 씁니다. 화면마다 테이블을 새로 짜면 25벌의 페이징 버그가 생깁니다.

```
config/screens.js        경로 ↔ 화면 정의 레지스트리 (28종, 동적 import)
        │
        ▼
data/screens/*.js        화면별 컬럼 · 필터 · 합계 규칙 · 예시 데이터
        │
        ▼
views/ListScreenPage.vue → components/common/ListScreen.vue   ← 동작은 여기 한 곳
```

**화면을 추가할 때 컴포넌트를 만들지 않습니다.** `data/screens/` 에 정의 하나를 더하고
`config/screens.js` 에 등록하면 끝입니다. 라우트는 `screenRoutes` 에서 파생되므로
라우터도 건드리지 않습니다.

대가로 `ListScreen.vue` 가 커졌습니다. 화면 하나가 이 틀을 못 맞추면
(Purchase PO · Inventory 처럼) 전용 컴포넌트로 빼는 편이 낫습니다 — 실제로 그 둘은
`components/purchase-po/` · `components/inventory/` 에 따로 있습니다.

---

## 4. 레이아웃 — 왜 양쪽이 드로어인가

한때 좌측 요약 레일과 우측 메뉴가 각각 220px 씩 **상시** 차지했습니다. 1,366px 노트북
(현지 표준 장비)에서 본문에 남는 폭이 900px 남짓이라, 목록 표의 열이 잘리고 가로
스크롤이 생겼습니다. ASM 은 화면의 대부분이 표인 시스템이라 이 손실이 치명적입니다.

그래서 둘 다 **off-canvas 드로어**로 바꾸고 본문에 폭을 전부 돌려줬습니다
(커밋 `8fc3338`).

- 한 번에 하나만 열립니다 — 좁은 화면에서 양쪽이 동시에 덮이면 본문이 사라집니다
- 열면 포커스가 드로어 안으로 들어가고, 닫으면 열었던 버튼으로 돌아옵니다
  (`useDrawerFocus`) — 드로어가 흐름 밖(fixed)이라 Tab 이 본문으로 새기 때문입니다
- 열려 있는 동안 배경 스크롤을 잠급니다 (`useBodyScrollLock`)
- 992px 미만에서는 헤더의 대분류 메뉴가 숨겨지므로, 드로어 안에 대분류 칩을 둡니다

헤더 좌측 로고는 **브랜드 메뉴 버튼**을 겸합니다. 요약·알림 아이콘을 헤더에 늘어놓지
않고 한 곳에 모아, 디자인가이드 4-2 가 금지하는 "로고 영역에 겹치는 아이콘·배지" 를
자연히 피합니다.

---

## 5. CSR 모듈 — 왜 격리했는가

`/csr` 은 Notion 에서 넘겨받은 개선요청을 관리하는 화면입니다. 운영 화면 25종과
**컴포넌트·스토어를 공유하지 않습니다**(`src/modules/csr/` 아래 독립).

이유는 수명이 다르기 때문입니다. 운영 화면은 언젠가 실서버 코드베이스로 들어가지만,
CSR 은 이 프로젝트가 끝나면 함께 끝납니다. 얽어 두면 나중에 떼어내기 어렵습니다.

### 권한은 DB 가 통제합니다

프론트의 `CSR_POLICY` 는 **UI 힌트일 뿐**입니다. 실제 차단은 두 겹입니다.

```
RLS 정책              →  어떤 '행' 을 다룰 수 있는가        (db/002)
컬럼 가드 트리거       →  어떤 '컬럼' 을 바꿀 수 있는가      (db/003)
```

RLS 는 컬럼 단위 조건을 표현할 수 없어서 둘로 나눴습니다. `it_dept` 계정으로
`verification_result` 를 바꾸려 하면 트리거가 `42501` 로 거부합니다 — 브라우저 개발자
도구로 UI 비활성을 풀어도 막힙니다.

**설계상 짚어 둔 것 세 가지**

- `csr_role()` 은 `SECURITY DEFINER` 입니다. `csr_user_roles` 에도 RLS 가 걸려 있고 그
  정책이 다시 `csr_role()` 을 부르므로, 호출자 권한이면 무한 재귀가 됩니다.
- 컬럼 가드는 **실제로 값이 달라진 컬럼만** 셉니다. 폼 전체를 되보내도 안 바뀐 값은
  위반이 아닙니다 — 그러지 않으면 `it_dept` 가 회신만 고쳐도 거부됩니다.
- 상태 로그 트리거도 `SECURITY DEFINER` 입니다. `csr_status_log` 에는 INSERT 정책을
  두지 않아 Data API 로는 아무도 로그를 쓰거나 지울 수 없고, 적재는 트리거만 합니다.

### 가입 제한이 없다는 점

Neon Auth(Better Auth 기반)는 2026-09-10 현재 **가입 제한을 지원하지 않습니다** —
누구나 가입할 수 있습니다. 지시서 §6 의 「회원가입 비활성」은 지킬 수 없습니다.

대신 모든 SELECT 정책이 `csr_role() IS NOT NULL` 이라, `csr_user_roles` 에 없는 사용자는
**가입해도 이슈를 한 건도 보지 못합니다.** 남는 위험은 데이터 노출이 아니라 계정 증가이며,
운영 중 Users 목록을 주기적으로 정리해야 합니다.

---

## 6. 배포

```
main 푸시 → GitHub Actions → vite build (ASM_BASE=/asm/) → dist/
                              └ cp dist/index.html dist/404.html   ← SPA 새로고침 대응
                            → GitHub Pages
```

라우터는 `createWebHistory(import.meta.env.BASE_URL)` 입니다. History 모드라 `/asm/csr` 로
직접 들어오면 Pages 가 404 를 돌려주는데, `404.html` 을 `index.html` 사본으로 두어 SPA 가
받아 처리합니다. **해시 라우터를 쓰지 않는 이유**가 이것입니다 — 실서버가 History 모드라
경로 형태를 맞춰야 합니다.

API 주소는 3단계로 결정합니다(`api/api-base.js`): 빌드타임 env → 런타임
`public/app-config.js` → same-origin. 배포 파이프라인이 백엔드 주소를 런타임 파일에
주입할 수 있어, 프론트를 다시 빌드하지 않고도 대상 서버를 바꿀 수 있습니다.

---

## 7. 지금 상태와 한계

**시드 데이터로 돌아갑니다.** `data/` 의 예시 행을 Pinia 스토어가 그대로 씁니다. 실서버
연동 시 스토어의 데이터 출처만 교체하면 화면 코드는 건드리지 않아도 됩니다 — 그러라고
스토어를 사이에 뒀습니다.

**로그인이 꺼져 있습니다.** 백엔드 세션 없이 화면만 배포하므로 `/auth` 는 `/` 로
리다이렉트되고 모든 라우트가 `requiresAuth: false` 입니다. `AuthPage.vue` ·
`stores/session.js` · `api/auth.js` 는 남겨 두었으니 라우트만 복구하면 되살아납니다.

**테스트가 없습니다.** 도입한다면 `utils/format.js`(숫자 표기 규칙)와 `stores/*.js`
(필터·정렬·페이징 파생값)가 먼저입니다 — 둘 다 순수 함수에 가깝고 회귀가 눈에 잘 띄지
않는 영역입니다.

**서버 사이드 페이징이 없습니다.** 현재는 전량을 받아 클라이언트에서 자릅니다.
1,000건을 넘기면 바꿔야 합니다.
